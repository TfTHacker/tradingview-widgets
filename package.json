{
  "name": "tradingview-widgets",
  "version": "0.1.0",
  "description": "Render TradingView widgets from safe Obsidian code blocks.",
  "main": "main.js",
  "scripts": {
    "dev": "node esbuild.config.mjs",
    "build": "tsc -noEmit -skipLibCheck && node esbuild.config.mjs production",
    "version": "node version-bump.mjs && git add package.json package-lock.json manifest.json versions.json",
    "postversion": "git tag -d v$npm_package_version 2>/dev/null; git tag $npm_package_version HEAD"
  },
  "keywords": [
    "obsidian",
    "tradingview",
    "widgets"
  ],
  "author": "TfTHacker",
  "license": "MIT",
  "devDependencies": {
    "@types/node": "^20.11.30",
    "builtin-modules": "^3.3.0",
    "esbuild": "^0.20.2",
    "obsidian": "latest",
    "tslib": "^2.6.2",
    "typescript": "^5.4.3"
  },
  "dependencies": {
    "yaml": "^2.4.1"
  }
}
